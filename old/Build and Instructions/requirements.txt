# ─────────────────────────────────────────────
#  VideoForge — requirements.txt
#  Instalar con:  pip install -r requirements.txt
# ─────────────────────────────────────────────

# Servidor web
flask

# Peticiones HTTP (Modal, Grok, APIs externas)
requests

# Procesamiento de imágenes
Pillow

# Whisper local (transcripción de audio)
openai-whisper

# Whisper acelerado (alternativa)
faster-whisper

# Cliente OpenAI (Whisper API)
openai

# Automatización de navegador (Grok login, Whisk)
playwright

# Base de datos MySQL (auth_module)
pymysql

# Hash de contraseñas (auth_module)
bcrypt

# PyInstaller (compilar el .exe — solo en máquina de build)
pyinstaller

# ─────────────────────────────────────────────
#  NOTAS:
#  1. PyTorch (requerido por openai-whisper):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
#  2. Después de instalar playwright:
#     playwright install chromium
#
#  3. FFmpeg NO es un paquete pip.
#     Descargar desde https://ffmpeg.org y agregar al PATH
#     o incluir ffmpeg.exe en el bundle del instalador.
# ─────────────────────────────────────────────
